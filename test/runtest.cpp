#include "../src/matmul.h" 
int main() {
    Matrix m1 = Matrix::identity(100);
    Matrix m2 = Matrix::identity(100);
    //std::cout << m1.mat_mul(m2).repr() << std::endl; 
    std::cout << "hi" << std::endl; 
}